# One module per report stage, each holding the per-run data-loading logic
# extracted from the matching report/*.qmd file's `_load_*` functions --
# see build_cache.py and report/README.md's "Report data caching" section.
